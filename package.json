{
  "name": "gravity-flip",
  "version": "1.0.0",
  "description": "Gravity Flip — neon gravity-flip arcade runner with BALL CLAW skin machine",
  "type": "module",
  "scripts": {
    "copy": "node scripts/copy-www.mjs",
    "patch": "node scripts/patch-android.mjs",
    "sync": "npm run copy && npx cap sync android && npm run patch"
  },
  "private": true,
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "cordova-plugin-purchase": "^13.12.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
